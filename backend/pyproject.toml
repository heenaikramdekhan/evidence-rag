[project]
name = "evidence-rag"
version = "0.1.0"
description = "Hybrid-retrieval RAG with reranking and enforced citations."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"
